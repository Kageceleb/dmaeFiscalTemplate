import { FlatList, Text, View } from "react-native";
import { s } from "./styles.ts";

export default function OsMenu(){
    return (
        <View style={s.menu}>
        <Text>Aqui vai a flatlist</Text>
        </View>
    )
}